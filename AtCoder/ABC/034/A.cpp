#include <bits/stdc++.h>

#define INF 1e9
using namespace std;
typedef long long ll;

int main() {

    int x, y; cin >> x >> y;

    if (x < y) {
        puts("Better");
    } else {
        puts("Worse");
    }
}