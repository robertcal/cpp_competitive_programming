#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
const int INF = 1e9;
const int MOD = 1e9 + 7;
const ll LINF = 1e18;

int main() {

    int n, m, q; cin >> n >> m >> q;

    vector<vector<int>> cnt(n + 10);
    for (int i = 0; i < m; ++i) {
        int l, r; cin >> l >> r;
        --l; --r;

    }
}