#include <bits/stdc++.h>

#define INF 1e9
using namespace std;
typedef long long ll;

int main() {

    ll h, w; cin >> h >> w;

    vector<string> c(h);
    for (ll i = 0; i < h; ++i) {
        cin >> c[i];

        cout << c[i] << endl;
        cout << c[i] << endl;
    }
}